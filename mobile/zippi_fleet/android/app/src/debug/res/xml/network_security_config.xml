<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG ONLY. Allows plain HTTP to a developer's Laravel server:

    10.0.2.2       the Android emulator's alias for the host machine
    localhost      adb reverse tcp:8000 tcp:8000
    192.168.x.x    a real handset on the same Wi-Fi

  ⚠ Deliberately scoped to those hosts rather than a blanket
  cleartextTrafficPermitted="true" — a blanket flag here is how cleartext ends
  up allowed to arbitrary hosts and nobody notices.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.246</domain>
        <domain includeSubdomains="true">172.16.4.21</domain>
    </domain-config>
</network-security-config>
